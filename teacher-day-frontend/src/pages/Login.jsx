import { useState } from 'react'
import { Navigate, useNavigate } from 'react-router-dom'
import { login } from '../services/api'
export default function Login(){const nav=useNavigate();const [form,setForm]=useState({email:'otambe655@gmail.com',password:''});const [error,setError]=useState('');if(localStorage.getItem('teacher_day_token')) return <Navigate to="/admin"/>;const submit=async e=>{e.preventDefault();try{const {data}=await login(form);localStorage.setItem('teacher_day_token',data.access_token);nav('/admin')}catch{setError('Unable to sign in. Check your credentials.')}};return <main className="admin-shell"><form className="admin-panel auth-panel" onSubmit={submit}><p className="eyebrow">Teacher's Day · Admin</p><h1>Teacher's Day Cards</h1><p>Manage eight personal greetings and their permanent QR links.</p>{error&&<div className="form-error">{error}</div>}<label>Email<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><button className="button button-dark">Sign in</button></form></main>}
